# webapp environment variables
# Copy to .env and set VITE_API_URL to point to your FastAPI server

VITE_API_URL=http://localhost:8080

# For Vercel deployment, set this in the Vercel project settings as an
# Environment Variable (do NOT commit your .env to git)
